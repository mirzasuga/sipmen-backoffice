/* eslint-disable */
const state = {
  layout: 'dashboard-layout'
}

const getters = {}

const actions = {}

const mutations = {
  setLayout(state, layout) {
    state.layout = layout
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
